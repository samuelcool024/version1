1. initialization with- git init   #done cone in the beginning of the project

2. staging area- 
    (a) git add filename   -for single file
    (b) git add .    -for all files

to commit- git commit -m "first commit message"

    note: commits can only be done after a code or modification or correction is done to a file.

push: 
    git push -u origin main
    note: this will push the local repo to the remote github repo

git pull  --to push to origin main/master or version branch


git clone paste reo url --to get cloned files


git checkout -b and <banch name> --to dreate and enter new branch

git checkout main --to go back to main branch


